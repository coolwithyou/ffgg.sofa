# 토스 페이먼츠 정기결제 시스템 통합

## 개요

SOFA 프로젝트에 토스 페이먼츠 기반 정기결제 시스템을 구현합니다. 이 문서는 전체 통합 계획의 개요와 각 Phase에 대한 요약을 제공합니다.

## 핵심 결정 사항

| 항목 | 결정 |
|------|------|
| **결제 권한** | admin 역할만 구독 관리 가능 |
| **서비스 제한** | 점진적 제한 정책 (past_due → suspended → expired) |
| **가격 정책** | 원화(KRW) 전용 |

### 가격표

| 플랜 | 월간 가격 | 대상 |
|------|----------|------|
| Basic | ₩13,000/월 | 소규모 비즈니스 |
| Standard | ₩65,000/월 | 중간 규모 비즈니스 |
| Premium | ₩260,000/월 | 대규모 비즈니스 |

## Phase 구성

### [Phase 1: 기반 구축](./phase-1-foundation.md)
- DB 스키마 설계 및 마이그레이션
- 환경변수 설정
- 빌링키 암호화 유틸리티
- 플랜 시드 데이터

### [Phase 2: 토스 클라이언트](./phase-2-toss-client.md)
- 토스 API 클라이언트 구현
- 웹훅 보안 검증
- 에러 코드 매핑

### [Phase 3: API 엔드포인트](./phase-3-api-endpoints.md)
- 구독 관리 API
- 결제 API
- 웹훅 엔드포인트

### [Phase 4: Inngest 함수](./phase-4-inngest.md)
- 결제 처리 함수
- 결제 재시도 함수
- 웹훅 처리 함수
- 알림 함수

### [Phase 5: 크론 작업](./phase-5-cron.md)
- 정기결제 트리거
- 만료 구독 처리
- Vercel Cron 설정

### [Phase 6: 클라이언트 UI](./phase-6-client-ui.md)
- 토스 SDK 연동
- 구독 페이지 개선
- 결제 플로우 페이지
- 결제 내역 페이지

### [Phase 7: 서비스 제한](./phase-7-service-restriction.md)
- 구독 상태별 제한 정책
- 미들웨어 적용

## 아키텍처 다이어그램

```
┌─────────────────────────────────────────────────────────────────┐
│                         클라이언트                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐  │
│  │ 플랜 선택    │  │ 결제 수단   │  │ 구독 관리 페이지     │  │
│  │ 페이지       │  │ 등록 UI     │  │ (상태/내역/취소)     │  │
│  └──────┬───────┘  └──────┬───────┘  └──────────┬───────────┘  │
└─────────┼─────────────────┼─────────────────────┼──────────────┘
          │                 │                     │
          ▼                 ▼                     ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Next.js API Routes                          │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ /api/billing/*                                              │ │
│  │  - subscription (조회/생성)                                 │ │
│  │  - billing-key (발급/삭제)                                  │ │
│  │  - plan/change (플랜 변경)                                  │ │
│  │  - payments (결제 내역)                                     │ │
│  │  - cancel (구독 취소)                                       │ │
│  └────────────────────────────────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ /api/webhooks/tosspayments                                  │ │
│  │  - HMAC 서명 검증                                           │ │
│  │  - 멱등성 체크                                              │ │
│  │  - Inngest 이벤트 발송                                      │ │
│  └────────────────────────────────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ /api/cron/*                                                 │ │
│  │  - process-billing (매일 00:05 UTC)                         │ │
│  │  - expire-subscriptions (매일 00:30 UTC)                    │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
          │                                   │
          ▼                                   ▼
┌─────────────────────┐           ┌──────────────────────────────┐
│   토스 페이먼츠      │           │        Inngest               │
│  ┌───────────────┐  │           │  ┌────────────────────────┐  │
│  │ 빌링키 발급   │  │           │  │ process-billing-payment│  │
│  │ 자동결제      │  │◄─────────►│  │ billing-retry          │  │
│  │ 결제 취소     │  │           │  │ billing-webhook        │  │
│  │ 웹훅 발송     │  │           │  │ billing-notification   │  │
│  └───────────────┘  │           │  └────────────────────────┘  │
└─────────────────────┘           └──────────────────────────────┘
                                              │
                                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      PostgreSQL (Neon)                           │
│  ┌──────────┐ ┌──────────────┐ ┌──────────┐ ┌────────────────┐  │
│  │  plans   │ │ subscriptions│ │ payments │ │ webhook_logs   │  │
│  └──────────┘ └──────────────┘ └──────────┘ └────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

## 구독 상태 플로우

```
                    ┌─────────────┐
                    │   pending   │ ◄── 구독 생성 (빌링키 미등록)
                    └──────┬──────┘
                           │ 빌링키 발급 완료
                           ▼
                    ┌─────────────┐
      ┌────────────►│   active    │◄────────────┐
      │             └──────┬──────┘             │
      │                    │ 결제 실패          │ 결제 성공
      │                    ▼                    │
      │             ┌─────────────┐             │
      │             │  past_due   │─────────────┘
      │             └──────┬──────┘
      │                    │ 7일 경과 후 계속 실패
      │                    ▼
      │             ┌─────────────┐
      │             │  suspended  │
      │             └──────┬──────┘
      │                    │
      │    ┌───────────────┼───────────────┐
      │    │               │               │
      │    ▼               ▼               ▼
      │ 결제 성공     30일 경과     사용자 취소
      │    │               │               │
      │    │        ┌──────▼──────┐ ┌──────▼──────┐
      └────┘        │   expired   │ │  canceled   │
                    └─────────────┘ └─────────────┘
```

## 재시도 전략

```
결제 실패 발생
    │
    ▼
┌─────────────────────────────────────────────────────────────┐
│ 1차 재시도: 4시간 후                                         │
│   └─ 성공 → active                                          │
│   └─ 실패 → 2차 재시도 예약                                  │
├─────────────────────────────────────────────────────────────┤
│ 2차 재시도: 24시간 후 (1일)                                  │
│   └─ 성공 → active                                          │
│   └─ 실패 → 3차 재시도 예약, past_due 상태 전환              │
├─────────────────────────────────────────────────────────────┤
│ 3차 재시도: 72시간 후 (3일)                                  │
│   └─ 성공 → active                                          │
│   └─ 실패 → suspended 상태 전환, 관리자 알림                 │
└─────────────────────────────────────────────────────────────┘
```

## 파일 구조

```
lib/
├── crypto/
│   └── billing.ts              # 빌링키 암호화
├── toss/
│   ├── client.ts               # API 클라이언트
│   ├── sdk.ts                  # 프론트 SDK
│   ├── webhook-security.ts     # 웹훅 보안
│   ├── error-mapping.ts        # 에러 매핑
│   └── types.ts                # 타입 정의
├── billing/
│   ├── subscription-policy.ts  # 구독 정책
│   ├── order-id.ts             # 주문 ID 생성
│   └── notifications.ts        # 알림 처리

app/api/
├── billing/
│   ├── subscription/
│   │   ├── route.ts            # GET: 조회
│   │   └── create/route.ts     # POST: 생성
│   ├── billing-key/route.ts    # POST/DELETE
│   ├── plan/
│   │   └── change/route.ts     # POST: 변경
│   ├── cancel/route.ts         # POST: 취소
│   ├── payments/route.ts       # GET: 내역
│   └── plans/route.ts          # GET: 플랜 목록
├── webhooks/
│   └── tosspayments/route.ts   # 웹훅 수신
└── cron/
    ├── process-billing/route.ts
    └── expire-subscriptions/route.ts

inngest/functions/
├── process-billing-payment.ts
├── billing-retry.ts
├── process-billing-webhook.ts
└── billing-notification.ts

app/(portal)/
├── billing/
│   ├── page.tsx                # 플랜 선택
│   ├── success/page.tsx        # 결제 성공
│   ├── fail/page.tsx           # 결제 실패
│   └── recovery/page.tsx       # 결제 복구
└── mypage/
    ├── subscription/page.tsx   # 구독 관리 (수정)
    └── payments/page.tsx       # 결제 내역 (신규)

drizzle/
├── schema.ts                   # 테이블 추가
└── seed/
    └── plans.ts                # 플랜 시드
```

## 구현 일정 (권장)

| 주차 | Phase | 작업 내용 |
|------|-------|----------|
| 1주차 | Phase 1 | DB 스키마, 환경설정, 암호화 유틸리티 |
| 2주차 | Phase 2-3 | 토스 클라이언트, API 엔드포인트 |
| 3주차 | Phase 4-5 | Inngest 함수, 크론 작업 |
| 4주차 | Phase 6 | 클라이언트 UI |
| 5주차 | Phase 7 | 서비스 제한 + 통합 테스트 |

## 테스트 체크리스트

### 빌링키 관련
- [ ] 빌링키 발급 성공
- [ ] 빌링키 발급 실패 (카드 정보 오류)
- [ ] 빌링키 삭제
- [ ] 빌링키 재등록

### 결제 관련
- [ ] 정기결제 성공
- [ ] 정기결제 실패 (잔액 부족)
- [ ] 재시도 후 성공
- [ ] 최종 실패 후 suspended 전환

### 웹훅 관련
- [ ] 웹훅 수신 및 처리
- [ ] 웹훅 서명 검증 실패 거부
- [ ] 중복 웹훅 멱등성 처리

### 구독 관리
- [ ] 플랜 업그레이드
- [ ] 플랜 다운그레이드
- [ ] 구독 취소 (즉시)
- [ ] 구독 취소 (기간 만료 시)

### 서비스 제한
- [ ] past_due 상태에서 제한 적용
- [ ] suspended 상태에서 읽기 전용
- [ ] 결제 복구 후 제한 해제

## 보안 고려사항

1. **빌링키 암호화**: AES-256-GCM으로 암호화하여 DB에 저장
2. **웹훅 검증**: HMAC SHA-256 서명 + 타임스탬프(5분) 검증
3. **API 인증**: admin 역할만 결제 관련 API 접근 가능
4. **환경변수**: 민감한 키는 환경변수로만 관리
5. **멱등성**: 웹훅 eventId로 중복 처리 방지

## 참고 자료

- [토스페이먼츠 개발자 문서](https://docs.tosspayments.com/)
- [토스페이먼츠 빌링키 발급 가이드](https://docs.tosspayments.com/guides/v2/billing)
- [토스페이먼츠 웹훅 가이드](https://docs.tosspayments.com/guides/webhook)
